# An escape sequence character is a backshash'\' followed by a character you want to insert.
# To insert characters that cannot be directly used in a string, we use an escape sequence character.

print("Hello,\nI am Nandan.") #here '\n is an escape sequence which breaks the line'

print("I am a \"good boy\"") #\" as we can't directly use it "